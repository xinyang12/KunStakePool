const { expect } = require('chai');
 
let Box;
let box;
 
// Start test block
describe('KunStakePool (proxy)', function () {
  beforeEach(async function () {
    // Kun = await ethers.getContractFactory("MockERC20");

    // kun = await Kun.deploy("Kun", "Kun");

    // await kun.deployed();

    // KunStakePool = await ethers.getContractFactory("KunStakePool");
    // let sTime = 1604160000;
    // const [owner] = await ethers.getSigners();
    // pool = await upgrades.deployProxy(KunStakePool, [await owner.getAddress(), kun.address, Number(sTime)], {initializer: 'initialize', unsafeAllowCustomTypes: true});
  });
 
  // Test case
  // it('retrieve returns a value previously initialized', async function () {
  //   // Test if the returned value is the same one
  //   // Note that we need to use strings to compare the 256 bit integers
  //   // expect((await box.retrieve()).toString()).to.equal('42');
  // });
});